# v0.9 interrogation

The pstack interrogation reviewed `origin/main...HEAD` before corrective commits `ae83bb2` through `de69849`.

| Reviewer | Findings |
| --- | --- |
| gpt-5.6-sol | 7 total: 1 critical, 6 warnings |
| gpt-5.5 | 3 total: 1 critical, 1 warning, 1 nit |
| gpt-5.4 | 2 warnings |
| gpt-5.6-luna | 3 warnings |

## Acted on

The panel produced seven unique issues after deduplication. All seven had concrete evidence and received proportional fixes.

1. Declare Linux runtime and build dependencies, document them, and inspect package metadata during release CI.
2. Keep PipeWire's synthetic default for capture while excluding it from microphone choices, and demote playback sinks.
3. Let an already available setup plan activate the matching engine and model configuration.
4. Use a generic ready summary and call the disclosed component count available rather than active.
5. Remove a concrete plan equal to Recommended and avoid repeating the visible Parakeet choice in Advanced.
6. Replace the one-time responsive check with a pinned Chromium test in the focused verifier and CI.
7. Move the new microphone and setup rendering out of `App.tsx` into cohesive Settings modules.

## Considered

None.

## Dismissed

None. Reviewers assigned different severities to overlapping symptoms, but every unique issue was actionable.
